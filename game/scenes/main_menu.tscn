[gd_scene load_steps=2 format=3]

; The first screen (UIX-02). Built in code, like the other panels: the scene is only the hook.

[ext_resource type="Script" path="res://scripts/UI/Menus/MainMenu.cs" id="1_menu"]

[node name="MainMenu" type="Control"]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_menu")
